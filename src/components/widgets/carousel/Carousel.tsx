import 'Carousel.css'
import React, {ReactNode} from "react";

interface CarouselProps {
    data: object;
    children?: ReactNode;
}

const Carousel: React.FC<CarouselProps> = ({data, children} : CarouselProps) => {
        

    return(
        <>
        </>
    )
}

export default Carousel